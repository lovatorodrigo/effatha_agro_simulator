pluginManagement {
    // Descobre o caminho do Flutter SDK (ENV ou local.properties)
    def flutterSdkPath = System.getenv("FLUTTER_SDK")
    if (!flutterSdkPath) {
        def props = new Properties()
        def f = new File(rootDir, "local.properties")
        if (f.exists()) {
            f.withInputStream { props.load(it) }
            flutterSdkPath = props.getProperty("flutter.sdk")
        }
    }
    assert flutterSdkPath != null : "Flutter SDK não encontrado. Defina FLUTTER_SDK ou adicione flutter.sdk ao android/local.properties"

    includeBuild("$flutterSdkPath/packages/flutter_tools/gradle")

    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "com.android.application" version "8.6.0"
        id "org.jetbrains.kotlin.android" version "1.9.24"
        id "dev.flutter.flutter-plugin-loader" version "1.0.0"
    }
}

dependencyResolutionManagement {
    // NÃO usar FAIL_ON_PROJECT_REPOS aqui
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        // Adiciona o repositório do engine do Flutter no nível de settings,
        // assim o plugin não precisa injetar no projeto.
        maven { url = uri("$flutterSdkPath/bin/cache/artifacts/engine") }
    }
}

rootProject.name = "effatha_agro_simulator"
include(":app")
